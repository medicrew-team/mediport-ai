name: Simple CI

on:
  push:
    branches:
      - develop

jobs:
  simple-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Simple health check
        run: echo "Mediport-AI CI OK"
